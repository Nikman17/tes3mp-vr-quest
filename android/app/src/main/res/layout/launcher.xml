<?xml version="1.0" encoding="utf-8"?>
<ScrollView xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#1a1a2e">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:padding="20dp">

        <!-- Title -->
        <TextView
            android:id="@+id/launcher_title"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="TES3MP VR"
            android:textSize="28sp"
            android:textStyle="bold"
            android:textColor="#e6b800"
            android:gravity="center"
            android:layout_marginBottom="6dp" />

        <TextView
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="OpenMW-VR + TES3MP"
            android:textSize="13sp"
            android:textColor="#888888"
            android:gravity="center"
            android:layout_marginBottom="20dp" />

        <!-- ── GAME DATA ── -->
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:background="#0d0d1a"
            android:padding="14dp"
            android:layout_marginBottom="12dp">

            <TextView
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:text="GAME DATA"
                android:textSize="11sp"
                android:textColor="#e6b800"
                android:textStyle="bold"
                android:layout_marginBottom="6dp" />

            <TextView
                android:id="@+id/game_data_path"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:text="(not configured)"
                android:textSize="12sp"
                android:textColor="#ff9800"
                android:maxLines="2"
                android:ellipsize="end"
                android:layout_marginBottom="10dp" />

            <Button
                android:id="@+id/select_data_button"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:text="📂  Browse for Morrowind Folder…"
                android:textSize="13sp"
                android:layout_marginBottom="6dp" />

            <Button
                android:id="@+id/manage_mods_button"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:text="Manage Mods / Content Files"
                android:textSize="13sp" />

        </LinearLayout>

        <!-- ── SP / MP SWITCH ── -->
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:background="#0d0d1a"
            android:padding="14dp"
            android:layout_marginBottom="12dp">

            <TextView
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:text="MODE"
                android:textSize="11sp"
                android:textColor="#e6b800"
                android:textStyle="bold"
                android:layout_marginBottom="8dp" />

            <RadioGroup
                android:id="@+id/mode_radio_group"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:orientation="horizontal">

                <RadioButton
                    android:id="@+id/radio_singleplayer"
                    android:layout_width="0dp"
                    android:layout_height="wrap_content"
                    android:layout_weight="1"
                    android:text="Singleplayer"
                    android:textColor="#cccccc"
                    android:checked="true" />

                <RadioButton
                    android:id="@+id/radio_multiplayer"
                    android:layout_width="0dp"
                    android:layout_height="wrap_content"
                    android:layout_weight="1"
                    android:text="Multiplayer (TES3MP)"
                    android:textColor="#cccccc" />

            </RadioGroup>

        </LinearLayout>

        <!-- ── SERVER IP (shown only in MP mode) ── -->
        <LinearLayout
            android:id="@+id/server_section"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:background="#0d0d1a"
            android:padding="14dp"
            android:layout_marginBottom="12dp"
            android:visibility="gone">

            <TextView
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:text="SERVER"
                android:textSize="11sp"
                android:textColor="#e6b800"
                android:textStyle="bold"
                android:layout_marginBottom="8dp" />

            <LinearLayout
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:orientation="horizontal"
                android:layout_marginBottom="6dp">

                <TextView
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:text="IP: "
                    android:textColor="#cccccc"
                    android:textSize="13sp"
                    android:gravity="center_vertical"
                    android:minWidth="30dp" />

                <EditText
                    android:id="@+id/server_ip_edit"
                    android:layout_width="0dp"
                    android:layout_height="wrap_content"
                    android:layout_weight="1"
                    android:hint="192.168.1.100"
                    android:textColor="#ffffff"
                    android:textColorHint="#666666"
                    android:textSize="13sp"
                    android:inputType="textUri"
                    android:maxLines="1"
                    android:imeOptions="actionDone"
                    android:background="#1e1e2e"
                    android:padding="8dp" />

            </LinearLayout>

            <LinearLayout
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:orientation="horizontal">

                <TextView
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:text="Port: "
                    android:textColor="#cccccc"
                    android:textSize="13sp"
                    android:gravity="center_vertical"
                    android:minWidth="30dp" />

                <EditText
                    android:id="@+id/server_port_edit"
                    android:layout_width="0dp"
                    android:layout_height="wrap_content"
                    android:layout_weight="1"
                    android:hint="25565"
                    android:textColor="#ffffff"
                    android:textColorHint="#666666"
                    android:textSize="13sp"
                    android:inputType="number"
                    android:maxLines="1"
                    android:imeOptions="actionDone"
                    android:background="#1e1e2e"
                    android:padding="8dp" />

            </LinearLayout>

        </LinearLayout>

        <!-- ── SETTINGS ── -->
        <Button
            android:id="@+id/settings_button"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="Settings"
            android:textSize="13sp"
            android:layout_marginBottom="12dp" />

        <!-- ── LAUNCH ── -->
        <Button
            android:id="@+id/launch_game_button"
            android:layout_width="match_parent"
            android:layout_height="56dp"
            android:text="LAUNCH"
            android:textSize="18sp"
            android:textStyle="bold"
            android:textColor="#000000"
            android:backgroundTint="#e6b800"
            android:layout_marginBottom="8dp" />

        <!-- ── STATUS ── -->
        <TextView
            android:id="@+id/status_message"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text=""
            android:textSize="12sp"
            android:textColor="#ff9800"
            android:gravity="center" />

    </LinearLayout>
</ScrollView>
